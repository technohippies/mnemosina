import ProgressBar from '../components/ProgressBar';

const meta = {
    title: 'Primitives/ProgressBar',
    component: ProgressBar,
};

export default meta;

export const Zero = {
    args: {
        value: 0,
        max: 100,
    },
};